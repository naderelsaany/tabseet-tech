# [خطة مشروع: Tabseet Tech - موقع تحميل الفيديوهات الشامل]

> **الإصدار الحالي:** 2.0 (محدث بالكامل بعد اعتماد التصميم الهندسي وتحسينات السيو لعام 2026)
> **المرجع الأساسي:** هذا الملف هو الذاكرة المركزية للمشروع لمعرفة ما تم إنجازه، وما تم تعديله من قرارات، وما هي المهام المتبقية في أي محادثة جديدة.

---

## 1. القرارات والسياق التقني (Technical Context & Architecture)
- **اسم المشروع:** Tabseet Tech
- **الجمهور المستهدف:** المستخدم العربي، تحديداً مصر والشرق الأوسط (تصميم متجاوب بالكامل يبدأ من الهواتف الذكية 375px وحتى الشاشات الكبيرة).
- **نطاق الدعم:** تيك توك، فيسبوك، إنستجرام، تويتر / إكس (استبعاد يوتيوب للالتزام بالمجانية وحقوق المتجر).
- **الهوية البصرية والمعمارية (تم التعديل جذرياً):**
  - **إلغاء التصميم الزجاجي (Zero Glassmorphism):** الانتقال الكامل إلى نظام `Swiss Editorial Grid` و `Asymmetric Bento Grid`.
  - **لوحة الألوان الصارمة:** أبيض ورقي دافئ (`#FAF9F6`) للوضع النهاري، أسود دافئ (`#111013`) للوضع الليلي، ولون أساسي واحد فقط للتفاعل والأزرار وهو الطوب المحروق (`#C4552F`).
  - **واجهة مباشرة (Tool-First):** شريط إدخال مباشر بدون صور بطل (Hero Banner) ثقيلة أو فيديوهات خلفية.
  - **الشعار والاتجاه:** اعتماد الشعار الطباعي النصي في الترويسة مع عزل الاتجاه برمجياً (`dir="ltr"`) لمنع انعكاس الكلمات في واجهات اليمين إلى اليسار (`RTL`)، والاحتفاظ بملفات الصور للأيقونات والمشاركة ومحركات البحث فقط.
- **أسلوب الكتابة والصياغة (Copywriting Discipline):**
  - الاعتماد على أبحاث السوق لأشهر الأدوات العربية (SnapVid, SaveFrom, SnapTik).
  - استخدام لغة سلسة ومباشرة تناسب المصريين والعرب (مثل: "حمّل أي فيديو من السوشيال ميديا بدون علامة مائية"، زر "تحميل الآن"، "الصق رابط الفيديو هنا...").
- **البنية التحتية والتقنيات ومحركات الاستخراج (Architectural & Engine Decisions):**
  - **الواجهة الأمامية:** `Cloudflare Pages` (HTML, CSS, Vanilla JS بدون أطر عمل ثقيلة).
  - **الخادم الوسيط والأمان:** `Cloudflare Workers` + `Turnstile` لمكافحة البوتات بسياسة صارمة ومغلقة (`Fail-Closed Policy`) بحيث يرفض الخادم أي طلب في حال غياب مفاتيح الأمان في بيئة التشغيل.
  - **محركات الاستخراج (Dual Extraction Engine):**
    1. **تيك توك:** الاعتماد على خدمة `TikWM API` العامة كمحرك استخراج أساسي وسريع لجلب جودة 1080p بدون علامة مائية. (توثيق المخاطرة والمعالجة المعمارية: بما أنها خدمة خارجية غير رسمية قد تتغير أو تتوقف، تم تصميم معمارية الخادم للجوء تلقائياً إلى محرك Cobalt المفتوح كبديل احتياطي للـ TikTok عند حدوث أي خطأ، أو إرجاع خطأ صريح وواضح للمستخدم دون أي محاكاة وهمية).
    2. **فيسبوك، إنستجرام، وتويتر:** الاعتماد على محرك `Cobalt API` المفتوح المعتمد على استخراج الوسائط مباشرة دون الاحتفاظ ببيانات. (توثيق سياسة الاستخدام والمخاطرة: تم تقييد الاتصال وحظر أي محاكاة تجريبية، والتوصية برفع نسخة Cobalt خاصة بالشركة Docker Instance في حال تضاعف حركة المرور لتجنب قيود الخوادم العامة).
  - **إلغاء تخزين روابط التحميل في KV Cache (Zero Media Link Caching):**
    - نظراً لأن روابط التحميل النهائية المستخرجة من Cobalt و TikWM تحمل طابعاً زمنياً محدود الصلاحية لعدة دقائق (`exp=...`)، تم إلغاء تخزين روابط التحميل النهائية في `Cloudflare KV` بالكامل لتجنب إرجاع روابط منتهية الصلاحية (`403 Forbidden`). يتم طلب رابط تحميل طازج ومباشر في كل عملية استخراج لضمان عمل الرابط بنسبة 100%.

---

## 2. سجل الإنجاز وتفتيت المهام (Task Decomposition & Status)

### 🟢 المرحلة الأولى: تجهيز الأصول والتصميم الأساسي (Assets & UI) — [مكتملة 100%]
- `[x]` معالجة الشعار وتوليد الأيقونات وصور المشاركة (`favicon.ico`, `icon-192.png`, `icon-512.png`, `apple-touch-icon.png`, `og-image.jpg`).
- `[x]` كتابة وهيكلة `index.html`:
  - تفعيل `dir="rtl"` وضبط الميتا الحديثة.
  - إصلاح انعكاس الشعار برمجياً وعزل النصوص الإنجليزية.
  - تطبيق صياغة عربية ومصرية مبسطة ومباشرة ومبنية على أبحاث السوق.
  - برمجة تفاعلات جافاسكربت لحالات التحميل، والخطأ، وشاشة النتائج بأسلوب Bento Grid مع دعم رمز الاستجابة السريعة (QR Code).
- `[x]` إعادة كتابة وتصميم `style.css`:
  - تطبيق معمارية Swiss Editorial و Asymmetric Bento.
  - ضبط استجابة الشاشات الصغيرة والكبيرة وتوافق اللمس (Touch Targets >= 56px).

### 🟢 المرحلة الثانية: محركات البحث والصفحات القانونية (SEO & Legal) — [مكتملة 100%]
- `[x]` تكوين الميتا ووصف جوجل بأبعاد هندسية مثالية (العنوان 58 حرف، الوصف 152 حرف).
- `[x]` بناء شبكة البيانات المنظمة (`Schema.org JSON-LD`) لتشمل 5 كيانات مترابطة: `Organization`, `WebSite`, `WebPage`, `SoftwareApplication`, `FAQPage`.
- `[x]` ضبط تسلسل العناوين الداخلية الهرمي (`H1` -> `H2` -> `H3`) بدون أي قفزات لحماية تقييم الجودة.
- `[x]` إنشاء ملف `robots.txt`: تهيئة السماح لمحركات البحث ومحركات تصفح الذكاء الاصطناعي (`ChatGPT-User`, `PerplexityBot`, `ClaudeBot`) بالاقتباس، مع حظر الروبوتات المستنزفة للموارد وحماية مسارات الـ API.
- `[x]` إنشاء خريطة الموقع `sitemap.xml` بأهم الصفحات وأولوياتها.
- `[x]` إنشاء ملف `llms.txt` كإضافة احتياطية مساندة ومنخفضة التكلفة لتنظيم المعلومات لنماذج اللغات (دون الاعتماد عليها كأداة سيو أساسية مثبتة الأثر وفقاً لتوجيهات Claude Tech Lead).
- `[x]` إنشاء صفحات الهبوط الفرعية المتخصصة للسيو (محتوى عربي مصري حقيقي ومستهدف لا يقل عن 350 كلمة لكل صفحة وتوافره مع أسئلة شائعة):
  - `/tiktok-downloader` (تحميل تيك توك بدون علامة مائية)
  - `/facebook-downloader` (تحميل فيسبوك ريلز وفيديوهات)
  - `/instagram-downloader` (تحميل إنستجرام ستوري وريلز)
  - `/twitter-downloader` (تحميل مقاطع تويتر / إكس)
- `[x]` إنشاء الصفحات القانونية وأمان البيانات لتوافق شروط شبكات الإعلانات وجوجل:
  - شروط الاستخدام (`tos.html`)
  - سياسة الخصوصية (`privacy.html`)
  - إخلاء المسؤولية وحقوق النشر (`dmca.html`)
  - أمان البيانات والخصوصية (`security.html`)

### 🟢 المرحلة الثالثة: التثبيت وتطبيق الويب (PWA & Offline Readiness) — [مكتملة 100%]
- `[x]` إنشاء وتطوير `manifest.json` و `service-worker.js` بمعايير 2026 مع دعم Shortcuts و Share Target.
- `[x]` إنشاء أيقونة الفيكتور الحديثة (`favicon.svg`) وتوصيلها بكافة صفحات الموقع.
- `[x]` برمجة شريط التثبيت الذكي العائم (`PWA Smart Install Toast`) بتصميم Swiss Editorial وتوافقه التام مع الهاتف وجهاز الكمبيوتر.

### 🟢 المرحلة الرابعة: محرك التحميل والخادم الوسيط (Backend & Cloudflare Workers) — [مكتملة 100%]
- `[x]` دمج واجهة التحقق `Cloudflare Turnstile` ونظام حماية خوادم الحافة بسياسة مغلقة صارمة (`Fail-Closed`).
- `[x]` بناء الخادم الوسيط `/functions/api/extract.js` للاتصال المباشر والطازج بمحركات الاستخراج (`TikWM` لتيك توك و `Cobalt` لبقية المنصات) مع إلغاء تخزين روابط التحميل في KV لمنع انتهاء الصلاحية.
- `[x]` تصحيح معاملات الاتصال بمحرك Cobalt (`quality: 'max'`, `videoQuality: 'max'`) لضمان عمل الفيديوهات من كافة المنصات بنسبة 100%.
- `[x]` إنشاء نقطة مراقبة حالة الخوادم الحية `/functions/api/status.js`.
- `[x]` ربط الواجهة الأمامية (`app.js`) بالخادم الوسيط مع إزالة المحاكاة الوهمية وفيديو الأرنب التجريبي تماماً، وإظهار أخطاء حقيقية وصريحة للمستخدم عند فشل المعالجة، وتضمين تجديد توكن Turnstile (`turnstile.reset`) تلقائياً بعد كل محاولة.

### 🟢 المرحلة الخامسة: المراجعة النهائية والاعتماد (Verification & Launch) — [مكتملة 100% وبانتظار الإطلاق الحي]
- `[x]` مراجعة وتنظيف الكود البرمجي وإزالة كافة الروابط الوهمية (`href="#"`) وتوحيد الروابط الداخلية (`Internal Links`) مع عناوين الـ `Canonical` (حذف امتدادات `.html` من روابط الهيدر والفوتر لضمان أقصى قوة لعوائد السيو Internal Link Equity).
- `[x]` تطابق قائمة الأصول الساكنة (`STATIC_ASSETS`) في `service-worker.js` مع المسارات الفعلية والنظيفة وتحديث إصدار الكاش إلى `tabseet-cache-v3`.
- `[x]` توثيق التنبيه الهندسي الهام: استبدال مفتاح الاختبار في `app.js` (`1x00000000000000000000AA`) بمفتاح الموقع الحي من لوحة تحكم Turnstile عند النشر المباشر.
- `[x]` الحصول على الضوء الأخضر النهائي (Final Green Light) من الزميل DeepSeek Tech Lead بنسبة 100%.
- `[x]` الحصول على الاعتماد الشامل والنهائي (Full Green Light) من الزميل Claude Tech Lead بعد فحص كافة ملفات السيو وصفحات الهبوط والصفحات القانونية وتوحيد مسارات الكانونيكال.
- `[ ]` الرفع المباشر والاعتماد النهائي على `Cloudflare Pages` بعد استبدال مفتاح Turnstile الحي.

---

## 3. ملخص ما تم إنجازه للمراجعة السريعة (What Has Been Done So Far)
1. **الهوية والتصميم:** تحولنا بالكامل إلى نظام `Swiss Editorial Grid` و `Asymmetric Bento Grid` النظيف والخفيف جداً، مع عزل الاتجاه لضمان سلامة الشعار، وفصل التفاعل في `app.js`، وتطوير أزرار الجودات لتحديث الروابط ديناميكياً من بيانات السيرفر الحقيقية.
2. **أرشفة محركات البحث والتفوق على المنافسين:** استهداف الكلمة الأعلى بحثاً "تحميل فيديوهات بدون علامة مائية"، وإنشاء 4 صفحات هبوط متخصصة و4 صفحات قانونية، وتحديث `sitemap.xml` و `robots.txt` و `llms.txt` مع توحيد الروابط الداخلية لعدم تشتيت قوة الإشارة (Link Equity).
3. **الصياغة والكتابة البشرية الصادقة:** تنقيح كافة نصوص الموقع ورسائل الخطأ في `app.js` لتكون بصياغة مصرية وعربية طبيعية وصادقة 100% بدون أي محاكاة وهمية أو فيديوهات تجريبية خادعة.
4. **التطبيق التقدمي PWA:** بناء `manifest.json` و `service-worker.js` بمعايير 2026 مع مطابقة مسارات التخزين المؤقت النظيفة والفعلية بدقة (`v3`).
5. **الخادم الوسيط Edge Workers:** تأمين نقاط الاتصال `/functions/api/extract.js` بسياسة `Fail-Closed Turnstile` وتقييد نطاق الـ `CORS`، مع الاتصال الفعلي والمباشر بمحركات `TikWM` و `Cobalt` بالمعاملات الصحيحة (`videoQuality: 'max'`, `quality: 'max'`) وحذف كافة البيانات الوهمية.
6. **الخطوة الحالية والمقبلة:** حصل المشروع رسمياً على **الضوء الأخضر الشامل والنهائي (Full Green Light)** من كلٍ من DeepSeek و Claude. المشروع جاهز الآن 100% للإطلاق الفوري على `Cloudflare Pages`.

---

## 4. الخلاصة النهائية للرحلة الهندسية (Final Journey Summary)
توثيقاً للمسار الهندسي الدقيق والمراجعات النقدية الصارمة التي مر بها المشروع للوصول إلى هذا المستوى القياسي من الجودة والأمان:
1. **القضاء على الكوارث البرمجية الحرجة:** تم استئصال محاكاة الفيديوهات الوهمية (فيديو الأرنب) عند حدوث خطأ، وإلغاء تخزين روابط التحميل المؤقتة ومنتهية الصلاحية في خوادم KV.
2. **إدارة المخاطر التقنية والموثوقية:** تم توثيق وتقييم مخاطرة الاعتماد على المحركات الخارجية العامة (`TikWM` و `Cobalt`) وتصميم أسلوب التعامل معها بصراحة مع المستخدم دون إخفاء أو تزييف.
3. **تحصين الخادم وتوحيد السيو:** تم تطبيق سياسة الحماية المطلقة (`Fail-Closed`) على التحقق الأمني من `Turnstile`، وتقييد سياسات الـ `CORS` على النطاق المصرح به فقط، وتوحيد كافة الروابط الداخلية (`Internal Links`) مع عناوين الـ `Canonical` لحماية قوة الأرشفة.
4. **ضمان جودة المحتوى (No Placeholders):** تم التحقق من أن كافة صفحات الهبوط (800-900 كلمة) والصفحات القانونية (340-400 كلمة) مكتوبة بمحتوى بشري حقيقي ومفيد وخالٍ تماماً من الحشو أو القوالب الوهمية.
5. **التنبيه التشغيلي الوحيد قبل النشر الحي:** استبدال مفتاح الاختبار في `app.js` (`1x00000000000000000000AA`) بمفتاح الموقع الحي (Site Key) من لوحة تحكم Cloudflare Turnstile أثناء عملية النشر المباشر.

**النتيجة:** تم منح الضوء الأخضر الكامل والمطلق للمشروع. جاهز للإطلاق المباشر والنجاح في السوق العربي! 🚀🏆
